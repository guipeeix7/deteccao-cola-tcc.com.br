import { SweetDatePipe } from './sweet-date.pipe';

describe('SweetDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SweetDatePipe();
    expect(pipe).toBeTruthy();
  });
});
